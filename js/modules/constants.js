/**
 * Archivo maestro que relaciona cada codigo de pantalla con su configuracion.
 */
export const CONFIG_FILE = "config/screens.json";

/**
 * Tiempo usado para revisar cambios cuando una pantalla no define
 * refreshConfigSeconds en su JSON.
 */
export const DEFAULT_REFRESH_SECONDS = 60;
